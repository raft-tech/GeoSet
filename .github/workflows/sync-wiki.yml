name: Sync Wiki

on:
  push:
    branches:
      - main
    paths:
      - 'wiki/**'

jobs:
  sync-wiki:
    runs-on: ubuntu-latest

    steps:
      - name: Generate GeoSetBot token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Checkout main repo
        uses: actions/checkout@v4

      - name: Checkout wiki repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}.wiki
          path: wiki-repo
          token: ${{ steps.app-token.outputs.token }}

      - name: Sync wiki files
        run: |
          rsync -av --delete --exclude '.git' wiki/ wiki-repo/

      - name: Push changes to wiki
        run: |
          cd wiki-repo
          git config user.name "geoset-bot[bot]"
          git config user.email "geoset-bot[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No wiki changes to sync."
          else
            git commit -m "Sync wiki from main repo ($(git -C .. rev-parse --short HEAD))"
            git push
          fi
